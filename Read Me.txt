These are event logs generated with the GENA and PLG Log Tool.
Both Logs used the same model, which can be generated via the benchmark log.

The script section include scripts, which were used to homogenize, mark and annotate the logs with several error_reasons.
A script to evaluate the event logs, via conformance checking is included too.